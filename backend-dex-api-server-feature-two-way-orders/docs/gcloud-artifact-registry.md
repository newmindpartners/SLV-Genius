## Gcloud artifact registry docker auth

The bullboard image is stored in a private registry. In order to gain access one must have the correct GCP permissions, gcloud installed and follow the steps described [here](https://cloud.google.com/artifact-registry/docs/docker/authentication#gcloud-helper)
